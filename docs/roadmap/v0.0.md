# v0.0 — Каркас

**Цель:** пустое работающее Next.js Mini App + бот. БД создана. Деплой автоматический.

**Итерации:** 1, 2, 3, 4, 5.

---

## ✅ Итерация 1 — Базовая инициализация

- ✅ 1.1 `git init` + создать приватный GitHub-репозиторий `sporty-hockey`
- ✅ 1.2 Создать Next.js 16 app: `pnpm create next-app apps/miniapp --typescript --app --tailwind=false --no-eslint --src-dir`
- ✅ 1.3 Установить runtime-зависимости: `pnpm add @telegram-apps/sdk-react zustand @supabase/supabase-js @tanstack/react-query grammy zod`
- ✅ 1.4 Установить dev-зависимости: `pnpm add -D @types/node` (поставилось create-next-app'ом)
- ✅ 1.5 Создать структуру папок `src/{app,components,features,hooks,lib,store,theme,types,i18n}/` по [ARCHITECTURE.md](../ARCHITECTURE.md)
- ✅ 1.6 `tsconfig.json`: `"strict": true`, alias `"@/*": ["./src/*"]`
- ✅ 1.7 Скрипты `pnpm typecheck` (`tsc --noEmit`) и `pnpm build` работают
- ✅ 1.8 `.env.example` в репо со всеми переменными (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `BOT_TOKEN`, `BOT_WEBHOOK_SECRET`); `.env`* в `.gitignore`

## ✅ Итерация 2 — Mini App каркас

- ✅ 2.1 `src/app/layout.tsx` + `providers.tsx` — инициализация TG SDK (`init()`, `mountThemeParams()`, `bindThemeParamsCssVars()`, `expandViewport()`, `mountMiniApp()` → `setMiniAppHeaderColor('bg_color')`); fallback в dev — `console.warn`
- ✅ 2.2 Базовые роуты: `/`, `/events`, `/money`, `/squad`, `/profile`, `/onboarding`. Группа `(tabs)` объединяет первые 5 под общим layout с TabBar; `/onboarding` — без TabBar
- ✅ 2.3 `theme/colors.ts`, `theme/spacing.ts`, `theme/typography.ts`, `theme/radius.ts` — все привязки через `var(--tg-theme-*)`
- ✅ 2.4 UI-кит в `src/components/`: `Button`, `Input`, `Card`, `Screen`, `TabBar`, `Chip`, `Avatar`, `EmptyState`
- ✅ 2.5 Нижний `TabBar` с 5 ссылками (`/`, `/events`, `/squad`, `/money`, `/profile`); подсветка активного через `usePathname`
- ✅ 2.6 5 экранов под `(tabs)` + `/onboarding` рендерят только заголовок из `i18n`
- ✅ 2.7 `src/i18n/ru.ts` + `src/hooks/use-t.ts` (плоский dict-look-up)
- ✅ 2.8 TanStack Query Provider в `Providers` (внутри root layout)

## ✅ Итерация 3 — Supabase БД и клиент

- ✅ 3.1 Создан проект Supabase `SportyHockey` (ref `wzwpnwianozcqavfqvht`, eu-central-1, Postgres 17.6); URL и service-role в `apps/miniapp/.env.local`
- ✅ 3.2 Миграция `0001_init` со всеми 8 таблицами из [ARCHITECTURE.md](../ARCHITECTURE.md#модель-данных-v01) применена через MCP `apply_migration`
- ✅ 3.3 `apps/miniapp/src/lib/supabase-server.ts` — клиент `supabaseServer()` с service-role, `import 'server-only'`
- ✅ 3.4 Сгенерированы типы → `apps/miniapp/src/types/db.ts`; `pnpm typecheck` и `pnpm build` зелёные

## 🟡 Итерация 4 — Бот + auth

- ✅ 4.1 Бот создан в @BotFather, токен и `BOT_WEBHOOK_SECRET` в `apps/miniapp/.env.local`
- 4.2 `/newapp` в BotFather — привязать Mini App к домену Vercel (placeholder, обновим после деплоя)
- ✅ 4.3 `apps/miniapp/src/lib/bot.ts` — ленивый `getBot()` (grammy) с обработчиком `/start` (приветствие)
- ✅ 4.4 `apps/miniapp/src/app/api/bot/route.ts` — Telegram webhook handler через `webhookCallback(getBot(), 'std/http')`; проверка `X-Telegram-Bot-Api-Secret-Token`
- 4.5 Установить webhook: `pnpm set-webhook <public-url>` (скрипт `scripts/set-webhook.mjs` готов; запуск после деплоя)
- ✅ 4.6 `apps/miniapp/src/lib/telegram-verify.ts` — HMAC-проверка `initData` через `BOT_TOKEN`, отказ при истёкшем `auth_date` (>24ч)
- ✅ 4.7 `apps/miniapp/src/lib/auth.ts` — `requireUser(request)`: парсит `Authorization: tma <initData>`, валидирует HMAC, upsert в `users`, возвращает `AuthedUser`; на ошибке — `AuthError`
- ✅ 4.8 `apps/miniapp/src/lib/api-client.ts` (фронт) — `apiFetch<T>` прикладывает `Authorization: tma <initData>` из `retrieveRawInitData()`, кидает `ApiError` на не-2xx

## Итерация 5 — Деплой

- 5.1 `vercel link` — привязать репозиторий к Vercel
- 5.2 Env-переменные в Vercel Project Settings (production + preview): `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `BOT_TOKEN`, `BOT_WEBHOOK_SECRET`
- 5.3 Push в `main` → авто-деплой работает; PR создаёт preview-окружение
- 5.4 Обновить URL Mini App в @BotFather на финальный Vercel-домен; запустить `set-webhook`
- 5.5 Smoke-test: `/start` боту → клик «Open» → Mini App открывается → 5 пустых табов отображаются
- 5.6 README.md в корне репо: как поднять локально (1 минута до dev-сервера)
