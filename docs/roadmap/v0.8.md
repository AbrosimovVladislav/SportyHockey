# v0.8 — Перезапуск: живая команда и канал

Проект стоял на паузе с 2026-06-09 (последний коммит `p-170`), возобновлён 2026-09-17. Вся текущая работа идёт здесь. [v0.7](v0.7.md) **отложена и не трогается** — вернёмся к ней после v0.8 либо точечно перенесём оттуда нужные пункты.

Порядок: поднять проект (68) → сбросить данные и зайти «первым пользователем» (69) → анонсы в Telegram-канал (70) → роли admin / organizer / player (71) → первая реальная тренировка и игра с ребятами из команды.

**Итерации:** 68+ (сквозная нумерация; 67 «Полная обкатка MVP» остаётся зарезервированной в v0.7).

---

## ✅ Итерация 68 — Подъём проекта после паузы

Проект Supabase ушёл в auto-pause за время простоя. Поднимаем инфраструктуру, страхуемся от повторения и приводим репозиторий в порядок.

- ✅ 68.1 Supabase `wzwpnwianozcqavfqvht` восстановлен из паузы (2026-09-17), статус `ACTIVE_HEALTHY`. Данные на месте: 18 таблиц, 32 миграции, бакет `team-media` (34 объекта, ~58 МБ); 3 команды, 14 пользователей, 57 событий (16 игр), 239 финансовых операций.
- ✅ 68.2 Ручная проверка пользователем (2026-09-17): Mini App открывается, всё нормально.
- ✅ 68.3 Локальное окружение: `pnpm install`, `pnpm typecheck && pnpm build` — чисто. pnpm 12 потребовал явный `allowBuilds` в `pnpm-workspace.yaml` (sharp / unrs-resolver = false — то же, что уже было в `ignoredBuiltDependencies`). `.env.local` в рабочей копии по-прежнему нет — нужен только для `pnpm dev` и скриптов.
- ✅ 68.4 Тариф: организация Supabase переведена на Pro — auto-pause больше не применяется, появились ежедневные бэкапы. Закрывает хвост 66.2 из [v0.6](v0.6.md). Стоимость: Pro 25 $/мес на организацию + ~10 $/мес compute за каждый активный проект − 10 $ кредитов; при трёх активных проектах в организации это ~45 $/мес, доля SportyHockey — ~10 $.
- ✅ 68.5 Миграции выгружены в репозиторий: 32 файла в [supabase/migrations/](../../supabase/migrations/), каждый сверен с `supabase_migrations.schema_migrations` по md5 — совпадают побайтово. Дальше схема меняется только так: применили в Supabase → положили тот же SQL в репозиторий.
- ✅ 68.6 Документация приведена к коду (умеренно): [ARCHITECTURE.md](../ARCHITECTURE.md) переписан под фактическое состояние (18 таблиц, мультикомандность через `x-team-id`, API по доменам), навигация в [PRODUCT_BRIEF.md](../PRODUCT_BRIEF.md) и [ROADMAP.md](../ROADMAP.md), описание дизайн-системы и конвенция миграций в `CLAUDE.md`, ✅ у итераций 54 / 55 в [v0.5](v0.5.md) и у v0.6.

> RLS на всех таблицах выключен — **осознанное решение**, не трогаем. Security advisor Supabase показывает это как ERROR; игнорируем.

---

## 🟡 Итерация 69 — Сброс данных: вход «первым пользователем»

Обнуляем пользовательские данные, чтобы пройти путь с нуля: первый вход → онбординг → создание команды → наполнение. Схема, миграции и справочник площадок остаются.

**Что важно знать перед сбросом:**

- `venues` **не трогаем.** CRUD площадок в приложении нет (только `GET /api/venues`), а `venue_id` обязателен при создании события — без площадки новый пользователь не создаст ни одного события. Остаётся единственная запись «Большая арена» (1000 ₽ с игрока / 10 000 ₽ аренда) вместе с её фото в Storage.
- Сброс обратим: перед очисткой все таблицы копируются в отдельную схему внутри той же БД. Схема не входит в `public`, поэтому через API не видна.
- Файлы в Storage через SQL удалить нельзя (триггер `protect_objects_delete`) — только через Storage API. Осиротевшие файлы ничему не мешают.

**Шаги:**

- ✅ 69.1 Бэкап: схема `backup_20260917`, в ней копии всех 18 таблиц `public` (`create table … as select *`). Количество строк сверено с `public` по каждой таблице; у ролей `anon` / `authenticated` / `service_role` доступа к схеме нет.
- ✅ 69.2 (2026-09-17, вариант «полный сброс» подтверждён пользователем) Очистка одной командой `TRUNCATE` 17 таблиц: `users`, `teams`, `team_memberships`, `team_invites`, `team_join_requests`, `team_default_lines`, `team_default_sides`, `team_section_images`, `events`, `event_attendances`, `event_lineups`, `event_lines`, `event_penalties`, `result_points`, `result_point_links`, `finance_transactions`, `media_items`.
- ✅ 69.3 Проверка: во всех 17 таблицах 0 строк, в `venues` — 1, миграций 32, бакет `team-media` на месте (34 файла).
- ✅ 69.3a Фикс перед первым входом: активная команда хранится в `localStorage` (`sh:active-team`) и уходит на сервер заголовком `X-Team-Id`, а менялась только ручным переключением в профиле. После сброса на устройстве остаётся id удалённой команды → `requireOrganizer` отвечает 403 на любую мутацию организатора, и переключателем в профиле это не лечится (он игнорирует выбор «текущей» команды). [src/app/(tabs)/layout.tsx](<../../src/app/(tabs)/layout.tsx>) теперь до рендера экранов приводит сохранённое значение к реальному членству. Тот же баг ловил бы любого, кто вышел из команды или был удалён из неё. **Нужен деплой до первого входа.**
- ✅ 69.4 Первый вход пользователя (2026-09-17): онбординг пройден, создана команда «Wolves», профиль заполнен (номер, позиция, телефон). Фикс 69.3a задеплоен коммитом `p-171`.
- 69.5 Storage: 33 осиротевших файла (~56 МБ: медиа, аватары, лого, картинки разделов) — удалить через Storage API, когда будет локальный `.env.local`; папку `venues/` оставить. Не блокирует.
- 69.6 Удалить схему `backup_20260917`, когда новая команда обживётся и откат точно не понадобится.

---

## Направление версии — решено 2026-09-17

Берём оба варианта сразу: одна реальная команда («Wolves», ребята заводятся по мере готовности) **и** Telegram-канал команды как главный вход. Порядок: сначала анонсы в канал (70), затем роли (71), затем первая реальная тренировка и игра — всё, что мешает, идёт в пул ниже.

---

## 🟡 Итерация 70 — Анонсы в чат команды (группа или канал Telegram)

Бот публикует анонс события в канал команды: фото + текст + кнопка «Записаться». Кнопка открывает Mini App сразу на странице события; новичок по дороге автоматически вступает в команду и разрешает боту писать в личку.

**Как устроена связка (сверено с документацией Telegram Bot API и Mini Apps):**

- Mini App привязана к **боту**, не к каналу. К каналу привязывается только бот: его добавляют администратором с правом «Публикация сообщений» (`can_post_messages`), и его посты выглядят как посты самого канала.
- Кнопки типа `web_app` работают **только в личке с ботом** — в канале Telegram их не принимает. Поэтому в анонсе обычная url-кнопка со ссылкой на Main Mini App бота: `https://t.me/<bot>?startapp=<параметр>`. Параметр приходит в приложение как `start_param` / `tgWebAppStartParam`.
- Кто открыл Mini App по ссылке и ни разу не писал боту, личных сообщений от бота не получит. Лечится нативным запросом `requestWriteAccess` (Bot API 6.9+); признак «уже разрешил» — `allows_write_to_pm` в `initData`.
- Сейчас переход на событие из бота **не работает**: клиент понимает только `join_<token>`, а `event_<id>` открывает главную. Чиним в этой же итерации.

**Решения пользователя:** новичок из канала вступает в команду автоматически (кнопка несёт invite-токен); личные сообщения о новом событии остаются вместе с постом в канал; картинка анонса — готовое фото, а не генерируемый постер; публикация — при создании события (переключатель) и вручную кнопкой.

**Что нужно от пользователя до теста:** (1) у бота в @BotFather настроена Main Mini App — проверка: ссылка `https://t.me/<bot>?startapp=test` открывает приложение; (2) бот добавлен в группу команды (см. 70.8; для канала — администратором с правом публикации).

### ✅ 70.1 — Схема БД

Миграция `announce_channel` (Supabase + файл в `supabase/migrations/`, затем перегенерация `src/types/db.ts`):

- `teams.announce_chat_id bigint`, `teams.announce_chat_title text` — привязанный канал.
- `events.announce_chat_id bigint`, `events.announce_message_id bigint`, `events.announced_at timestamptz` — куда и когда ушёл анонс (нужно для повторной публикации, отмены и переноса).
- `users.bot_write_allowed boolean` — разрешил ли человек боту писать в личку.
- `team_section_images.section` — в CHECK добавляются `announce_training` и `announce_game` (свои картинки анонсов).
- Вторая миграция `teams_timezone`: `teams.timezone text` (IANA). **Найдено по ходу:** сервер форматировал время в сообщениях бота без пояса, то есть на Vercel — в UTC; время в личных карточках было сдвинуто на несколько часов, в анонсах было бы так же. Пояс заполняется автоматически с устройства организатора (создание команды и каждое создание события), запасной — `Europe/Moscow`. Форматирование вынесено в [src/lib/bot-format.ts](../../src/lib/bot-format.ts).

### ✅ 70.2 — Привязка канала

Без перенастройки webhook: организатор **пересылает боту в личку любой пост из канала**.

- Новый [src/lib/bot-channel.ts](../../src/lib/bot-channel.ts) (`bot.ts` уже 324 строки): обработчик сообщения с `forward_origin.type = 'channel'` → находит команду, где отправитель — организатор (после итерации 71 — админ) → через `getChatMember` проверяет, что бот в канале админ с `can_post_messages` → сохраняет `announce_chat_id` / `announce_chat_title` → отвечает «Канал «…» привязан к команде …». Понятные ответы на ошибки: бот не админ, нет права публикации, отправитель не организатор. Если команд несколько — выбор inline-кнопками. **Добавлено по ходу:** отправитель должен быть администратором самого канала — без этого чужой человек мог бы привязать к своей команде канал, где бот уже админ.
- `GET / DELETE /api/teams/me/channel` — статус и отвязка. Хук [src/hooks/use-team-channel.ts](../../src/hooks/use-team-channel.ts).
- UI: блок «Канал анонсов» в настройках команды, вкладка «Общее» — новый файл `announce-channel-block.tsx`: статус (привязан «…» / не привязан), инструкция в два шага, кнопка «Отвязать».

### ✅ 70.3 — Публикация анонса

- Новый [src/lib/announce.ts](../../src/lib/announce.ts): собирает подпись (тип, название или «Игра с «…»», дата и время, арена, взнос, комментарий из `events.details`), выбирает фото и шлёт `sendPhoto` в канал с url-кнопкой «Записаться». Фото сервер скачивает сам и загружает файлом (лимит 10 МБ против 5 МБ при отправке по URL). Если фото нет или оно не прошло — анонс уходит текстом.
- Порядок выбора фото: картинка анонса команды под тип события → фото площадки → стандартная картинка из `/public/`.
- Ссылка кнопки: `buildEventDeepLink(eventId, token)` в [src/lib/team-link.ts](../../src/lib/team-link.ts) → `https://t.me/<bot>?startapp=ev_<eventId>_<token>`.
- `POST /api/events/[id]/announce` (организатор) — опубликовать или опубликовать повторно; сохраняет `announce_message_id`. Хук `use-announce-event.ts`.
- `POST /api/events` принимает `announce` (по умолчанию `true`): личные сообщения уходят как раньше, плюс пост в канал, если канал привязан. Сбой анонса не ломает создание события — ответ несёт статус `sent / skipped / failed`, причину клиент показывает сообщением. Получение invite-токена вынесено в [src/lib/team-invite.ts](../../src/lib/team-invite.ts). У роутов с анонсом `maxDuration = 30`.
- UI: переключатель «Анонс в канал» в форме создания события (виден, только если канал привязан); пункт «Анонсировать в канал» / «Анонсировать повторно» в меню события.
- Две новые строки в блоке «Изображения разделов»: «Анонс тренировки», «Анонс игры».

### ✅ 70.4 — Переход по кнопке и вступление новичка

- [src/app/start-param-redirect.tsx](../../src/app/start-param-redirect.tsx): разбор `ev_<eventId>_<token>` → `POST /api/join/<token>` (идемпотентно: участника не трогает, новичка добавляет игроком) → активной становится эта команда → переход на `/events/<id>`.
- Новичок без подтверждённого профиля сначала проходит существующий шаг подтверждения в онбординге; целевой путь переживает его в `sessionStorage`, после подтверждения человек попадает на событие, а не на главную.
- Заодно чинятся старые кнопки бота: «Открыть в Mini App» в личной карточке события и «Открыть состав» в заявке об оплате открывают нужный экран напрямую (это личка, там `web_app` разрешён).

### ✅ 70.5 — Разрешение на личные сообщения

- Новый `src/app/write-access-prompt.tsx`, монтируется в `providers.tsx`: после загрузки профиля, если `allows_write_to_pm` не `true`, вызывает `requestWriteAccess` — нативное окно Telegram «Разрешить боту писать вам?». Результат уходит в `PATCH /api/me` → `users.bot_write_allowed`.
- При отказе не спрашиваем чаще раза в три дня (метка в `localStorage`).

### ✅ 70.6 — Отмена и перенос

Сейчас `notifyEventCancelled` / `notifyEventUpdated` — пустые заглушки. Если у события есть анонс в канале, бот отвечает на него реплаем: «❌ Отменено: <причина>», «🔁 Перенос: <новая дата и время>» (только при смене времени или площадки — правка взноса или деталей реплая не стоит) либо «✅ Событие снова в силе» при возврате отменённого. Личные сообщения об отмене и переносе — в кандидатах.

### ✅ 70.7 — Build check и документация

`pnpm typecheck && pnpm build`; обновить [features/notifications.md](../features/notifications.md) (там до сих пор описан удалённый cron-напоминатель) и [ARCHITECTURE.md](../ARCHITECTURE.md).

### ✅ 70.8 — Группа вместо канала (2026-09-17)

После первого деплоя выяснилось: у команды не канал, а **группа**. Публикация, кнопка «Записаться», авто-вступление, запрос на личку и реплаи от типа чата не зависят (хранится просто `chat_id`) — переделана только привязка, плюс три особенности групп. Сверено с документацией Telegram (Bot Features → Privacy Mode, Bot API → Message).

- **Привязка группы.** Пересланное из группы сообщение id чата не несёт, поэтому способ канала не годится. Вместо этого: (1) бота добавляют в группу — служебное сообщение `new_chat_members` бот получает всегда, даже в privacy mode; если добавил админ группы, управляющий командой, чат привязывается сразу; (2) запасной путь и перепривязка — команда `/connect@<бот>` в группе (адресованные боту команды доходят и в privacy mode). Webhook перенастраивать не нужно: это обычные `message`.
- **Страховка от случайного переноса.** Авто-привязка при добавлении срабатывает только для команды без привязанного чата; перенос анонсов в другую группу — только явным `/connect`.
- **Права.** Боту в группе достаточно быть участником, которому не запретили писать (админом — надёжнее: ограничения группы на медиа его не касаются). Привязывающий обязан быть админом группы и организатором в приложении; «анонимный админ» получает просьбу написать не анонимно.
- **Темы.** Миграция `teams_announce_thread` → `teams.announce_thread_id`: `/connect`, отправленный в теме форум-группы, направляет анонсы в эту тему.
- **Смена id чата.** При превращении группы в супергруппу Telegram меняет id: обрабатываем служебное `migrate_to_chat_id` (команда и уже опубликованные анонсы), а при публикации — ошибку с новым id (запоминаем и повторяем один раз).
- **UI.** Формулировки нейтральные: «Чат анонсов», «Анонс в чат команды», «Анонсировать в чат»; инструкция в настройках — под группу, канал — примечанием.

### Как это работает, когда команд много

Продумано 2026-09-17, схема уже так и реализована:

- **Один бот на все команды, у каждой команды — свой канал.** Привязка хранится в `teams.announce_chat_id`; бот может быть администратором в любом числе каналов. Анонс уходит в канал той команды, которой принадлежит событие. Перепривязка просто заменяет канал.
- **Одна Mini App на всех.** Кнопка «Записаться» несёт id события и invite-токен именно этой команды — человек попадает в нужную команду и на нужное событие, активная команда в приложении переключается сама.
- **Защита от чужих каналов.** Бот общий, поэтому при привязке проверяются обе стороны: отправитель управляет командой в приложении **и** является администратором самого канала. Иначе любой мог бы переслать пост из чужого канала, где бот уже админ, и слать туда свои анонсы.
- **Человек в нескольких командах.** Личная карточка события от бота показывает название команды; при привязке канала бот спрашивает, к какой из команд его привязать.
- **Часовые пояса.** Сервер живёт в UTC, команды могут быть в разных городах — время в сообщениях бота пишется в поясе команды (`teams.timezone`, берётся с устройства организатора автоматически).
- **Один канал на две команды** (клуб с двумя составами) технически возможен — ограничения нет, анонсы обеих команд пойдут в общий канал.
- **Группа или канал — на выбор команды:** у одних группа с обсуждением, у других канал только для объявлений; поддержаны оба, привязка отличается (см. 70.2 и 70.8).
- **Чего пока нет:** очередь отправки — Telegram ограничивает бота ~30 сообщениями в секунду, на сотнях команд личную рассылку придётся ставить в очередь; бот не узнаёт сам, что его убрали из чата (webhook не получает `my_chat_member`) — ошибка всплывёт при следующей публикации понятным текстом.

### Шаги тестирования 70

Код готов, `pnpm typecheck && pnpm build` — чисто; ждёт деплоя и прохода пользователем. Основной сценарий — **группа** (70.8); канал остаётся рабочим запасным вариантом.

1. Настройки команды → «Общее» → «Чат анонсов»: статус «не привязан», видна инструкция с командой `/connect@<бот>`.
2. Добавить бота в группу команды (ты — админ группы) → бот пишет в группу «Готово! Группа … привязана к команде Wolves». В настройках статус сменился на название группы.
3. Запасной путь: отвязать чат в настройках → отправить в группе `/connect@<бот>` → бот снова привязывает. В группе с темами — отправить команду в нужной теме и убедиться, что анонс приходит именно туда.
4. Негативные проверки: `/connect@<бот>` отправляет участник, который не админ группы или не организатор в приложении → понятный отказ, привязка не меняется. `/connect` в личке с ботом → подсказка, что команда работает в группе.
5. Создать тренировку с включённым «Анонс в чат команды» → в группе пост от бота: фото, текст с **правильным временем**, кнопка «Записаться»; в личку пришла карточка с «Иду / Не иду».
6. Нажать «Записаться» своим аккаунтом → Mini App открывается сразу на странице этой тренировки.
7. Попросить одного из ребят (не в команде, боту не писал) нажать «Записаться» → окно Telegram с разрешением писать → подтверждение профиля → страница события; в составе команды появился новый игрок.
8. Создать ещё одно событие → этому человеку пришло личное сообщение от бота (значит, разрешение сработало).
9. Создать игру с соперником → анонс «Игра: Wolves vs …»; загрузить картинку «Анонс игры» в «Изображения разделов», затем меню события → «Анонсировать повторно» → в группе второй пост уже с новой картинкой.
10. Перенести событие, затем отменить → под анонсом в группе два реплая: перенос и отмена.
11. Отвязать чат → новое событие создаётся без поста в группу, личные сообщения приходят как раньше.

---

## Итерация 71 — Роли: админ / организатор / игрок

Сейчас ролей две: `organizer` и `player`. Добавляем над ними `admin` — один на команду, создатель. **Решение пользователя:** организатор умеет всё, что умеет сейчас; только админ назначает и снимает организаторов, архивирует команду и привязывает канал.

### 71.1 — Схема БД

Миграция `team_memberships_admin_role`: CHECK роли → `admin / organizer / player`; частичный уникальный индекс «не больше одного админа на команду»; бэкфил — в каждой команде без админа самый ранний организатор становится админом (в «Wolves» это пользователь).

### 71.2 — Сервер

- [src/lib/role.ts](../../src/lib/role.ts): `MemberRole` с `admin`, хелпер «управляющая роль» (admin или organizer), `asMemberRole` понимает `admin`.
- [src/lib/auth.ts](../../src/lib/auth.ts): `requireOrganizer` пропускает админа и организатора — все существующие организаторские роуты работают для админа без правок; новый `requireAdmin`.
- `POST /api/teams`: создатель команды получает `admin`.
- `PATCH /api/teams/me/members/[user_id]`: менять роль может только админ; роль самого админа не меняется. `DELETE`: админа удалить нельзя; организатора удаляет только админ.
- `POST /api/teams/me/archive` и привязка / отвязка канала — только админ. `POST /api/teams/me/leave`: админ выйти не может (передача админства — в кандидатах).
- Проход по ~16 серверным файлам с литералом `'organizer'`: получатели заявки об оплате, проверки в заявках на вступление, удалении медиа и финансах — везде «управляющая роль» через хелпер.

### 71.3 — Клиент

- `useIsOrganizer` возвращает `true` и для админа (29 файлов с проверкой роли не трогаем); новый `useIsAdmin`.
- Редактирование игрока: выбор роли «Игрок / Организатор» виден только админу. Вкладка «Роли» и профиль: три ярлыка — «Админ», «Организатор», «Игрок». «Опасная зона» (архив) и отвязка канала — только у админа.
- i18n: ключи ролей.

### 71.4 — Build check и документация

`pnpm typecheck && pnpm build`; [features/team.md](../features/team.md), [ARCHITECTURE.md](../ARCHITECTURE.md) (поток авторизации), `src/types/db.ts`.

### Шаги тестирования 71

1. Профиль и вкладка «Роли»: у пользователя ярлык «Админ».
2. Открыть игрока → редактирование → назначить организатором → у него появились раздел «Деньги», создание событий, настройки команды.
3. Зайти этим организатором: в редактировании игроков **нет** выбора роли; нет архива команды и отвязки канала; удалить другого организатора нельзя.
4. Организатор создаёт событие, отмечает явку и оплату — работает как раньше.
5. Админ снимает роль организатора → человек снова игрок, организаторские разделы пропали.
6. Попытки удалить админа или сменить ему роль, выход админа из команды — понятный отказ.
7. Новый аккаунт создаёт свою команду → он в ней админ.
8. Игрок отправляет «Я оплатил» → уведомление получают и админ, и организаторы.

---

## Кандидаты

Не итерации, а пул: берём в работу по решению пользователя.

- **Хвосты анонсов (после 70).** Личные сообщения об отмене и переносе тем, кто голосовал; живой счётчик «идут N из M» прямо в посте канала; отметка в составе, кто не разрешил боту писать в личку (`users.bot_write_allowed` уже будет собираться); генерируемый постер вместо готового фото.
- **Передача админства (после 71).** Сейчас админ один и выйти из команды не может — нужна передача роли другому участнику.
- **CRUD площадок.** Реальной команде нужна своя арена с реальными ценами, а сейчас площадку можно завести или поправить только SQL-запросом. Для варианта A это почти обязательное условие. Эпик: [features/venues.md](../features/venues.md) (сейчас числится в post-MVP).
- **Хвосты прошлых версий.** 54.6 — сквозные фильтры из среза в ленту операций; заметки из [testing/02/06/26.md](../testing/02/06/26.md) — запрет повторной оплаты оплаченного события, модель депозита площадке по аналогии с игроками.
- **Локальный Supabase.** `supabase/config.toml` удалён в `p-106`, поэтому `supabase start` / `supabase db reset` из `CLAUDE.md` сейчас не работают. Вернуть, если понадобится локальная БД; для работы через облачный проект не нужно.
