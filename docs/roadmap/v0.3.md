# v0.3 — Прочие секции раздела «Команда»

**Цель:** закрыть оставшиеся секции раздела «Команда» поверх готового «Состава» (v0.2): главная раздела (хаб), Статистика, Тактика, Медиа, Настройки команды.

Раздел «Команда» (4-й нижний таб, бывший «Состав») получает **главную страницу-хаб** по образцу страницы события `/events/[id]`: шапка, сводка по команде, ближайшее событие и список секций. Сами секции (Статистика / Тактика / Медиа / Настройки) дозаполняются по итерациям; до реализации каждая открывает заглушку «Скоро».

**Итерации:** 36, 37, 38, 39, 40 (сквозная нумерация; v0.2 закрылась на 35). Порядок 37→40 предварительный — подвинем по фидбеку пилотов. Тактика идёт последней как наименее проработанное направление (добрейнштормим перед стартом).

---

## ✅ Итерация 36 — Главная раздела «Команда»

Эпик: [features/team.md](../features/team.md)

Таб «Состав» превращается в раздел «Команда» с промежуточным экраном-хабом. Текущий список состава становится одной из секций хаба.

- ✅ 36.1 Маршрутизация: корень таба `/squad` — теперь хаб; список состава переехал в `/squad/roster` (вместе с `lines-tab.tsx`, `sides-tab.tsx`). `/squad/[user_id]`, `/squad/[user_id]/edit`, `/squad/new` не менялись. Переходы из профиля, создания и удаления игрока ведут на `/squad/roster`. `isActive` нижней навигации покрывает все `/squad/*`.
- ✅ 36.2 Нижняя навигация: 4-й таб `/squad` переименован «Состав» → «Команда» (иконка и href прежние); первый таб `/` переименован «Команда» → «Главная» (убран дубль названия).
- ✅ 36.3 Хаб `/squad/page.tsx`: тёмная шапка `DarkHeader` с фоновой картинкой `/team.png` и заголовком «Команда» (название раздела, не имя команды). Количество игроков в шапку не выводится — оно показано в блоке-сводке ниже.
- ✅ 36.4 Блок-сводка по составу: компонент `team-stat-cells.tsx` — четыре ячейки (всего игроков, нападающие, защитники, вратари) с иконками набора Game Icons и вертикальными разделителями. Счётчики из `useTeamMembers` по `position`.
- ✅ 36.5 Карточка ближайшего события: переиспользует `EventCard`. Берётся ближайшее запланированное событие из `useEvents`, тап ведёт на `/events/[id]`. Если предстоящих событий нет, карточка скрыта.
- ✅ 36.6 Список секций — пять `ListRow`: «Состав» (→ `/squad/roster`), «Статистика и аналитика», «Тактика», «Медиа», «Настройки команды». Последние четыре открывают заглушку «Скоро» (`soon-sheet.tsx`).
- ✅ 36.7 i18n `team.*`. Список состава как подэкран получил кнопку «назад» в шапке. Хаб одинаков для организатора и игрока.

---

## Итерация 37 — Статистика и аналитика (командная)

Эпик: [features/stats.md](../features/stats.md)

Экран `/squad/stats` поверх готовой модели `result_points` (v0.2).

- Командные агрегаты по сезону: игры/тренировки, бомбардиры/ассистенты, посещаемость.
- Таблицы игроков с сортировками, раздельно тренировки/игры (как в профиле игрока v0.2).

---

## Итерация 38 — Медиа (общая галерея команды)

Эпик: [features/media.md](../features/media.md)

Экран `/squad/media` поверх готового медиа-функционала событий (v0.1).

- Лента всех медиа со всех событий команды в одном месте.
- Фильтр по событию/дате; открытие в полноразмерный просмотрщик.

---

## Итерация 39 — Настройки команды

Эпик: [features/team.md](../features/team.md)

Экран `/squad/settings`: параметры команды и фото для шапок.

- Параметры команды: название, логотип, роли/доступы.
- **Фото для шапок (`DarkHeader`: «Команда», «Расписание», событие).** Организатор загружает картинку для блоков шапки своей команды; сейчас зашиты статичные `/team.png`, `/bus.png`, `/arena.png`. Кадрировщик с фиксированной рамкой (как `AvatarCropper` из 28.4), на выходе нормализованная картинка.
  - Блок шапки рендерится `cover` на всю ширину × `252px + опасная зона` (≈ квадрат, чуть шире, зависит от устройства). Рекомендация загрузки: **4:5 (вертикальное), субъект по центру с запасом ~20% сверху/снизу, минимум ~1440×1800 px, JPG/WebP.** При 4:5 картинка всегда показывается на всю ширину (по бокам не режется), подрезается только верх/низ.

---

## Итерация 40 — Тактика

Эпик: [features/lineup.md](../features/lineup.md)

Экран `/squad/tactics` поверх дефолтных звеньев (v0.2). Наименее проработанное направление — детали зафиксируем после отдельного брейншторма.

- Библиотека схем/заготовок построений команды.
- Возможные наборы под разных соперников/ситуации.
