# v0.1 — PoC

**Цель:** проверить ядро гипотезы на 1–3 пилотных командах. Организатор ведёт события, явку и финансы в одном месте; игроки голосуют через бот без открытия Mini App.

**Итерации:** 6, 7, 8, 9, 10, 11, 12, 13.

---

## ✅ Итерация 6 — Онбординг + команда (минимально)

Эпики: [features/onboarding.md](../features/onboarding.md), [features/team.md](../features/team.md)

- ✅ 6.1 Welcome screen `/onboarding` — выбор роли (organizer / player)
- ✅ 6.2 Organizer flow: поле «Название команды» (без лого на PoC), кнопка «Создать команду»
- ✅ 6.3 API `POST /api/teams` — создаёт `team` + `team_membership(role=organizer)` для текущего user (zod-валидация: длина 2–50)
- ✅ 6.4 API `GET /api/me` — текущий user + список memberships (`team_id`, `team_name`, `role`) + `invite_link` (только для organizer)
- ✅ 6.5 Gate в `(tabs)/layout.tsx`: если у user нет memberships → `replace('/onboarding')`; в `/onboarding` — обратно `replace('/')` после получения membership
- ✅ 6.6 Player flow без ссылки: empty state «Попроси организатора прислать ссылку приглашения»
- ✅ 6.7 Bot: handler `/start team_<uuid>` — upsert user, создаёт `team_membership(role=player)` (или сообщает «уже в команде»), отвечает с inline-кнопкой «Открыть Mini App» (через `MINI_APP_URL`)
- ✅ 6.8 Squad screen `/squad` + API `GET /api/teams/me/members` — список членов команды (avatar, имя, @username, роль)
- ✅ 6.9 Profile screen `/profile` — карточка user, карточка команды; для organizer — кнопка «Скопировать ссылку приглашения» (формат `https://t.me/<BOT_USERNAME>?start=team_<uuid>`)

## ✅ Итерация 7 — События: API (CRUD)

Эпик: [features/events.md](../features/events.md)

UI событий — отдельной итерацией после прихода дизайн-системы и конкретных экранов.

- ✅ 7.1 `requireOrganizer(req)` в `src/lib/auth.ts` → `{ user, team_id }` или 403; находит team_id текущего user'а по `team_memberships(role=organizer)`
- ✅ 7.2 Заглушки нотификаций `src/lib/notify.ts`: `notifyEventCreated`, `notifyEventUpdated`, `notifyEventCancelled` — пустые функции с `// TODO: 11.x — реализовать рассылку`
- ✅ 7.3 API `GET /api/events` — события команды текущего user (по team_id), кроме `status=cancelled`; сортировка `starts_at ASC`; в каждом событии счётчик явки `{ going, maybe, not_going }` из `event_attendances`
- ✅ 7.4 API `POST /api/events` — organizer-only, zod-валидация (`type`, `starts_at`, `title?`, `ends_at?`, `venue_text?`, `cost_per_player?`, `description?`); создаёт event с `team_id`, `created_by`; вызывает `notifyEventCreated`
- ✅ 7.5 API `GET /api/events/[id]` — detail (поля события + счётчик явки); 404 если не в команде user'а
- ✅ 7.6 API `PATCH /api/events/[id]` — organizer-only; все поля optional + `status?` (только `scheduled` / `cancelled`); вызывает `notifyEventUpdated` или `notifyEventCancelled` в зависимости от изменения
- ✅ 7.7 Типы DTO в `src/types/api.ts`: `EventDto`, `EventDetailDto`, `CreateEventRequest`, `UpdateEventRequest`, `EventsListResponse`

## ✅ Итерация 8 — Дизайн-система v1

Источник: [`design_handoffs/dh_v1`](../../design_handoffs/dh_v1) — Manrope, брендовая светлая палитра, тёмный хедер «Расписание», карточки событий, состав/финансы. Подробная спецификация — [DESIGN.md](../DESIGN.md).

- ✅ 8.1 Токены в `src/theme/`: `colors.ts` (brand palette + 6 пар avatar-градиентов), `spacing.ts` (шкала 2/4/6/8/10/12/16/20/24/32/40/48), `radius.ts` (xs/sm/md/lg/xl/pill + RADIUS_FULL/RADIUS_SHEET), `typography.ts` (display/h1/h2/h3/body/bodyBold/sm/smBold/caption/label/score/stat)
- ✅ 8.2 Шрифт Manrope через `next/font/google` в `src/app/layout.tsx`; `globals.css` — белый фон, брендовый primary, `.pressable` с transform-фидбеком; отказ от `var(--tg-theme-*)`
- ✅ 8.3 Providers: `setMiniAppHeaderColor('#FFFFFF')` и `setMiniAppBackgroundColor('#FFFFFF')` чтобы TG-chrome был светлым независимо от темы пользователя
- ✅ 8.4 Перебор примитивов под DS: `Button` (primary/secondary/ghost/danger, size md/lg), `Input` (invalid state), `Card` (variant surface/warm/schedule), `Chip` (7 тонов), `Avatar` (градиентный, детерминистский по имени), `Screen`, `EmptyState`
- ✅ 8.5 Icons-набор `src/components/icons.tsx`: Home, Calendar, Ruble, People, More, Menu, Bell, Clock, Trophy, ChevronRight, Plus, Back, Check, Close, Person, Wallet, Location, Chat (outline, stroke 1.5–2.5)
- ✅ 8.6 `BottomNav` (замена `TabBar`) — 5 табов с иконками: **Команда / События / Деньги / Состав / Ещё**; активный — `headerAccent`, неактивный — `navInactive`
- ✅ 8.7 DS-блоки v3 (расписание): `DarkHeader`, `GlassButton`, `FAB` (dark/primary), `ContentTabs` (Список/Календарь), `FilterChips`, `SectionHeader`, `EventCard` (training/game/tournament, режимы today/week)
- ✅ 8.8 DS-блоки v2 (состав/финансы): `ActionTile` (50×50, Был/Сдал), `Donut`, `StatChip`, `PlayerRow`
- ✅ 8.9 Перевод существующих экранов (`/onboarding`, `/profile`, `/squad`, `(tabs)/layout.tsx`) на новые токены и `BottomNav`
- ✅ 8.10 [DESIGN.md](../DESIGN.md) — переписан под v1 (без статуса DRAFT); [ARCHITECTURE.md](../ARCHITECTURE.md) — обновлён список компонентов и упоминания TabBar → BottomNav

## Итерация 9 — Расписание (экран списка событий)

Источник: [`design_handoffs/dh_schedule_v1`](../../design_handoffs/dh_schedule_v1). Эпик: [features/events.md](../features/events.md).

- [ ] 9.1 Бэк: `GET /api/events` дополняется полем `team_size: number` в `EventsListResponse` — `count: 'exact', head: true` по `team_memberships(team_id)`; на клиенте служит знаменателем счётчика `count / total` в `EventCard`. `EventDto` без изменений.
- [ ] 9.2 Хук `src/hooks/use-events.ts` — `useEvents()` через TanStack Query (`queryKey: ['events']`, без `refetchInterval` на этой итерации).
- [ ] 9.3 Утилиты `src/lib/event-format.ts`: `formatTime(iso)` → `'19:30'`, `formatTodaySubtitle(date)` → `'24 мая, сб'`, `formatWeekDate(iso)` → `{ date: '26', day: 'пн' }`, `groupEvents(events, now)` → `{ today, week, later }` по локальной дате (today = текущий день, week = до конца текущей ISO-недели, later = всё дальше).
- [ ] 9.4 Хук `src/hooks/use-tg-header.ts` — `useTgHeader(color)`: при монтировании `setMiniAppHeaderColor(color)`, на размонтировании возвращает `'#FFFFFF'`. Нужно, чтобы TG-chrome над DarkHeader был тёмно-зелёным.
- [ ] 9.5 i18n: ключи `schedule.role.organizer|player`, `schedule.title`, `schedule.tabs.list|calendar`, `schedule.filters.all|training|game`, `schedule.sections.today|week|later`, `schedule.empty`, `schedule.calendarSoon`, `schedule.fabLabel`, `schedule.backLabel`.
- [ ] 9.6 Компонент `src/components/back-button.tsx` — `GlassButton` с `IconBack`, рендерится только если в `window.history.length > 1`; клик → `router.back()`. Переиспользуем на других экранах.
- [ ] 9.7 Страница `src/app/(tabs)/events/page.tsx` — полная вёрстка расписания:
  - `DarkHeader` (role «Капитан»/«Игрок» по `useMe`, title «Расписание», `left=<BackButton/>`, `right` — `IconBell` + зелёный dot 11×11 `success` с рамкой 2.5px `headerBg`, без круглого фона)
  - Белый sheet (`borderRadius: '24px 24px 0 0'`, `marginTop: -12`, `position: relative`, `zIndex: 2`)
  - `ContentTabs` Список/Календарь; Календарь → `EmptyState` «Скоро»
  - `FilterChips` Все/Тренировки/Игры (без турниров — нет в `events.type`)
  - Группы: «Сегодня» с subtitle = `formatTodaySubtitle(today)`, «Эта неделя», «Далее» (последняя без subtitle)
  - `EventCard mode=today` для сегодня, `mode=week` для остальных
  - Если после фильтрации событий нет → `EmptyState` «Событий пока нет»
  - `FAB variant=dark`, видим только organizer → `router.push('/events/new')` (страница появится в итерации 10; до тех пор — `alert('Скоро')`)
- [ ] 9.8 Рефакторинг `src/app/(tabs)/layout.tsx`: убрать общий `paddingBottom` обёртки (ломает full-bleed DarkHeader на events). `Screen` дополняется `paddingBottom: BOTTOM_NAV_HEIGHT + spacing['16']`, чтобы short-страницы (home/squad/profile/money) не уезжали под BottomNav.

## Итерация 10 — Создание события (форма `/events/new`)

Эпик: [features/events.md](../features/events.md). Дизайн прихожу отдельно.

- [ ] 10.1 Страница `src/app/(tabs)/events/new/page.tsx` — форма создания: тип (training/game), title?, starts_at, ends_at?, venue_text?, cost_per_player?, description?. Submit → `POST /api/events` → invalidate `['events']` → `router.replace('/events')`.
- [ ] 10.2 Доступ только organizer (`useMe`, redirect на `/events`, если player).

## Итерация 11 — Детали события + голосование + бот-пуши

Эпики: [features/voting-attendance.md](../features/voting-attendance.md), [features/notifications.md](../features/notifications.md). Дизайн details-страницы — отдельный handoff.

- [ ] 11.1 Страница `src/app/(tabs)/events/[id]/page.tsx` — деталь события (вся информация из `EventDetailDto`)
- [ ] 11.2 API `POST /api/attendance/vote` — upsert по `(event_id, user_id)`, поле `vote`
- [ ] 11.3 Vote block на странице деталей — три большие кнопки `+ / ? / −`, активная подсвечивается
- [ ] 11.4 Сводка явки: счётчик `+/?/−` + три списка проголосовавших
- [ ] 11.5 TanStack Query на странице деталей — `refetchInterval: 10000` пока страница открыта (фоновая актуализация явки)
- [ ] 11.6 Bot: после `POST /api/events` рассылает каждому игроку команды личное сообщение с inline-кнопками голосования
- [ ] 11.7 Bot: callback-handlers `vote:going|maybe|not_going:<event_id>` — пишет в БД, перерисовывает сообщение с подсветкой выбранной кнопки
- [ ] 11.8 Bot: рассылка при изменении события — каждому, кто проголосовал
- [ ] 11.9 Bot: рассылка при отмене события — каждому, кто голосовал `+` или `?`
- [ ] 11.10 Bot: команда `/events` — список ближайших событий команды с inline-кнопками голосования
- [ ] 11.11 Vercel Cron `/api/cron/voting-reminders` — каждый час; шлёт «голосование закрывается через N часов» тем, кто не ответил; N = 24ч hardcoded на PoC

## Итерация 12 — Состав на событие (страница roster)

Эпик: [features/roster-day.md](../features/roster-day.md)

- [ ] 12.1 Roster page `/events/[id]/roster` — три вкладки (`?tab=ops|split|stats`)
- [ ] 12.2 Вкладка «Оперативная»: список проголосовавших `+/?`, чекбокс «Пришёл», поле «Оплатил» (сумма)
- [ ] 12.3 API `PATCH /api/attendance` — обновление `showed_up`, `paid_amount` (только organizer)
- [ ] 12.4 При вводе оплаты автоматически создаётся `finance_transaction` (type=`player_payment`)
- [ ] 12.5 Вкладка «Деление» — для тренировки две кнопки белые/тёмные на каждого пришедшего, запись в `team_color`; для игры — заглушка «Будет в v0.2+»
- [ ] 12.6 Сводка под делением: «Белых: N, Тёмных: M»
- [ ] 12.7 Вкладка «Статистика» (мин.) — поля «Голы» и «Передачи» на каждого пришедшего, API `PATCH /api/attendance` (поля `goals`, `assists`)

## Итерация 13 — Финансы

Эпик: [features/finances.md](../features/finances.md)

- [ ] 13.1 Money screen `/money` — баланс команды (income − expense), список последних транзакций (последние 20)
- [ ] 13.2 Блок «Должники»: расчёт из событий где `showed_up=true AND paid_amount < cost_per_player`
- [ ] 13.3 Add transaction modal — форма со всеми типами (`income / expense / player_payment / player_debt`), сумма, описание, опциональная привязка к event и user
- [ ] 13.4 API `GET /api/finance` (баланс + транзакции) и `POST /api/finance` (создать транзакцию)
- [ ] 13.5 API `GET /api/finance/debts` — рассчитанный список должников
