# Эпик: События

## Цель
Создание, редактирование, просмотр тренировок и игр. Сердце продукта.

## Фичи

### v0.1 (API — итерация 7, UI — отдельной итерацией после дизайн-системы)
- **API события.** `GET /api/events` (список команды, без `cancelled`, со счётчиком явки), `POST /api/events` (organizer-only, zod), `GET /api/events/[id]` (detail + счётчик явки, 404 если user не в команде), `PATCH /api/events/[id]` (organizer-only, все поля optional + `status`).
- **Список событий (UI).** Группировка «Сегодня / Эта неделя / Дальше». Карточка: тип, дата, время, площадка, сводка явки (`3+ / 2? / 4−`).
- **Создание события (UI).** Форма: тип (тренировка/игра), дата, время начала/конца, площадка свободным текстом, стоимость на игрока, описание. По «Сохранить» → POST + бот рассылает голосование (рассылка в 8.x).
- **Страница события (UI, детали).** Шапка: тип, дата/время, площадка, описание. Блок явки ([voting-attendance.md](voting-attendance.md)). Плашка перехода на страницу состава ([roster-day.md](roster-day.md)). Заглушка «Медиа».
- **Редактирование события (UI).** Только организатор. При изменении даты/времени — бот пушит обновление (в 8.x).
- **Отмена события (UI).** Soft-delete (`status = cancelled`), рассылка отмены (в 8.x).

### v0.2
- Выбор площадки из справочника (см. [venues.md](venues.md))
- Календарный вид (toggle «Список / Календарь»)
- Дубликат события («Повторить как в прошлый раз»)

### v0.5+
- Публичные события (`visibility = public`), лендинг события
- Серии тренировок (recurring)

## Не входит на PoC
- Серии / recurring
- Календарный вид

## Связанные файлы
- `src/app/api/events/route.ts` — GET / POST
- `src/app/api/events/[id]/route.ts` — GET / PATCH
- `src/lib/event-enum.ts`, `src/lib/event-attendance.ts`, `src/lib/notify.ts`, `src/lib/user-team.ts`
- `src/lib/auth.ts` — `requireOrganizer`
