-- v0.8, итерация 70: часовой пояс команды (IANA, напр. 'Europe/Moscow').
-- Сервер (Vercel) живёт в UTC, а бот пишет людям дату и время события текстом —
-- без пояса команды время в анонсах и личных карточках уезжает на несколько часов.
-- Заполняется автоматически с устройства организатора при создании команды и события.
-- NULL — пояс ещё не известен, сервер использует запасной.
alter table public.teams
  add column if not exists timezone text;